backend:
  name: git-gateway
  branch: main  # or your default branch
  identity_url: https://peaceful-lily-1e98d1.netlify.app/.netlify/identity
  gateway_url: https://peaceful-lily-1e98d1.netlify.app/.netlify/git

media_folder: "products/media"  # where uploaded images go
public_folder: "/products/media"

collections:
  - name: "products"
    label: "Products"
    folder: "products"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Name", name: "name", widget: "string" }
      - { label: "Description", name: "description", widget: "text" }
      - { label: "Price", name: "price", widget: "number" }
      - { label: "Images", name: "images", widget: "list", allow_add: true, fields: [{label: "Image", name: "image", widget: "image"}] }
